
- url = product.new_record? ? products_path : product_path( product )

.products--form
  = form_for product, url: url do |f|
    .form-field
      %label Name
      = f.text_field :name

    .actions
      = f.submit 'Submit'

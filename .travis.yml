
services:
  - mongodb
language: ruby
rvm:
 - 2.3.1

cache: bundler 

before_script:
  - cd $TRAVIS_BUILD_DIR/test/dummy && bundle install --path vendor/bundle
  - cd $TRAVIS_BUILD_DIR/test/dummy && ls -lah vendor/bundle/ruby/2.3.0/bin

script: 
  - cd $TRAVIS_BUILD_DIR/test/dummy && bundle install && bundle install rspec && bundle exec rspec spec

